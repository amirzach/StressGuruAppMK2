import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amirzakariah.stressguru',
  appName: 'Stress Guru',
  webDir: 'build'
};

export default config;
